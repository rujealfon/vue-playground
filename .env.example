# API Configuration
VITE_API_BASE_URL=http://localhost:3001

# JWT Secrets (IMPORTANT: Change these in production!)
# Generate secure random strings for production use
# You can use: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-example
JWT_REFRESH_SECRET=your-super-secret-refresh-key-change-this-in-production-example

# Development Environment
NODE_ENV=development

# Optional: Database URL (for future real database integration)
# DATABASE_URL=postgresql://username:password@localhost:5432/blog_db

# Optional: Email Configuration (for password reset, notifications)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# Optional: File Upload Configuration
# UPLOAD_MAX_SIZE=10MB
# UPLOAD_ALLOWED_TYPES=jpg,jpeg,png,gif,pdf,doc,docx

# Optional: Redis Configuration (for session storage, caching)
# REDIS_URL=redis://localhost:6379
